# UI 需求列表

> 前端展示界面 — 爬取配置、职位浏览、批量投递

## 1. 全局配置页

| ID | 需求 | 优先级 |
|----|------|--------|
| 1.1 | 关键词输入：多关键词支持（逗号分隔），默认值从 `KEYWORDS` 读取 | P0 |
| 1.2 | 薪资范围输入：from/to 两个输入框，默认值 50000 / 120000 | P0 |
| 1.3 | 平台选择：JobsDB / CTGoodJobs 复选框 | P0 |
| 1.4 | 爬取数量输入：默认 100 | P1 |
| 1.5 | 通知邮箱配置：SMTP host/port/email/app_password | P1 |
| 1.6 | Cover Letter 路径配置（仅 JobsDB 需要） | P2 |
| 1.7 | 配置持久化：保存到本地文件，下次打开自动加载 | P0 |

## 2. 爬取控制

| ID | 需求 | 优先级 |
|----|------|--------|
| 2.1 | 「开始爬取」按钮：点击后调用后端爬虫，实时显示日志流 | P0 |
| 2.2 | 爬取进度条 / 关键词进度显示（当前正在爬: AI Agent 3/5） | P1 |
| 2.3 | 爬取日志实时输出区域（滚动终端风格） | P1 |
| 2.4 | 爬取完成摘要：每个关键词 新N / 更新M | P0 |
| 2.5 | 支持单独重爬某个关键词 | P2 |

## 3. 职位列表

| ID | 需求 | 优先级 |
|----|------|--------|
| 3.1 | 表格展示：编号、平台图标、职位标题、公司、薪资、发布时间、申请状态 | P0 |
| 3.2 | 按关键词分组 / 全部显示切换 | P1 |
| 3.3 | 筛选：按平台、关键词、薪资范围、发布时间、申请状态、can_auto_apply | P0 |
| 3.4 | 排序：按发布时间、薪资、公司名 | P1 |
| 3.5 | 分页：每页 20/50/100 可选 | P1 |
| 3.6 | 点击职位标题 → 在浏览器打开 job link | P0 |
| 3.7 | 显示 `last_apply_status` 状态标签（applied / unknown_questions / external / failed / skipped） | P0 |

## 4. 批量投递

> ⚠️ **注意**：刚爬取的职位 `can_auto_apply` 默认为 1（未知）。首次投递时脚本会逐个访问职位页检测模式（1-click / external），检测后才会标记真实值。检测本身不计入 `last_apply_status`。

| ID | 需求 | 优先级 |
|----|------|--------|
| 4.1 | 行前复选框：单选 / 全选 | P0 |
| 4.2 | 「批量投递」按钮：一次性投递所有选中职位 | P0 |
| 4.3 | 投递队列：逐个显示进度（[2/10] 正在投递: Senior Data Scientist — 回答雇主问题...） | P0 |
| 4.4 | 投递结果实时更新：每完成一个更新该行的 `last_apply_status` | P0 |
| 4.5 | 投递结果汇总：成功 N / 失败 N / 暂停(未知问题) N / 外链跳过 N | P1 |
| 4.6 | 遇到未知问题时暂停并提示用户（可在 UI 上填写答案后继续） | P2 |
| 4.7 | 申请前自动检测模式：首次点击「批量投递」时，脚本会逐个访问职位页判断 `1-click` / `external`，外链自动标记 `can_auto_apply=0` 并跳过投递 | P0 |
| 4.8 | 仅投递 `can_auto_apply=1` 的职位（外链已确认跳过的不再重试） | P1 |

## 5. 申请状态列

| ID | 需求 | 优先级 |
|----|------|--------|
| 5.1 | 用颜色/图标区分状态：🟢 applied / 🔵 pending / 🟡 unknown_questions / 🟠 external / 🔴 failed / ⚪ 未申请 | P0 |
| 5.2 | 按状态筛选（点击状态标签即筛选） | P1 |
| 5.3 | 显示上一次申请时间（`applied_at`） | P2 |
| 5.4 | 「重试全部待处理」按钮：重新投递所有 `unknown_questions` 状态的职位 | P1 |

## 6. 数据统计

| ID | 需求 | 优先级 |
|----|------|--------|
| 6.1 | 仪表盘概览：总活跃 / 今日新 / 已申请 / 外链 / 待处理 | P1 |
| 6.2 | 按关键词统计柱状图 | P2 |
| 6.3 | 每日新增职位趋势图 | P3 |

## 7. 技术选型（待定）

| 选项 | 方案 |
|------|------|
| 后端 | Python Flask / FastAPI（直接调用现有 crawl.py / apply_job.py） |
| 前端 | React / Vue / 纯 HTML+JS |
| 实时通信 | WebSocket（日志流）或 SSE（Server-Sent Events） |
| 数据库 | 直接读 SQLite（现有 jobs.db） |

---

## 后续补充

> 请在下方自行添加新需求
